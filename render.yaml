services:
  - type: web
    name: autonomous-ai-persona-java
    env: docker
    dockerfilePath: Dockerfile
    envVars:
      - key: GEMINI_API_KEY
        sync: false
      - key: PORT
        value: 3000
    disk:
      name: persona-data
      mountPath: /app/data
      sizeGB: 1
